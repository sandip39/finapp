import { ValidateequalDirective } from './validateequal.directive';

describe('ValidateequalDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateequalDirective();
    expect(directive).toBeTruthy();
  });
});
